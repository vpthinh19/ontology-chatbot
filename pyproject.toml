[project]
name = "ontchatbot"
version = "0.0.9"
description = "Vietnamese academic-procedure chatbot — PhoBERT NER + ontology-grounded retrieval"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "fastapi[standard]",
    "huggingface-hub",
    "transformers",
    "underthesea",
    "owlready2",
    "rapidfuzz",
]

[project.optional-dependencies]
inference = ["onnxruntime"]
train = [
    "torch",
    "accelerate",
    "onnxruntime-gpu",
    "onnxscript",
    "datasets",
    "seqeval",
    "matplotlib",
    "seaborn",
]

[project.scripts]
train = "ontchatbot.scripts.train:main"
evaluate = "ontchatbot.scripts.evaluate:main"
serve = "ontchatbot.scripts.serve:main"
bench_fuzzy = "ontchatbot.scripts.bench_fuzzy:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/ontchatbot"]

[tool.hatch.build.targets.wheel.force-include]
"resources" = "ontchatbot/resources"

[[tool.uv.index]]
name = "pytorch-cuda"
url = "https://download.pytorch.org/whl/cu128"
explicit = true

[tool.uv.sources]
torch = [
    { index = "pytorch-cuda", extra = "train", marker = "sys_platform != 'darwin'" }
]

[dependency-groups]
dev = ["pytest>=9.0.3"]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["src"]

[tool.uv]
package = true
# inference (onnxruntime) and train (onnxruntime-gpu) both provide the
# `onnxruntime` Python module — installing both would shadow one with
# the other. Force the user to pick exactly one.
conflicts = [
    [ { extra = "inference" }, { extra = "train" } ],
]
