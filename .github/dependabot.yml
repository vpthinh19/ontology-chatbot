# Weekly auto-PRs to keep deps + CI tooling fresh.
#
# - pip            tracks pyproject.toml; Dependabot reads uv.lock as of late 2024
# - github-actions tracks the `uses:` pins in .github/workflows/*.yml
# - docker         tracks the FROM lines in the Dockerfile

version: 2
updates:
  - package-ecosystem: pip
    directory: /
    schedule:
      interval: weekly
    open-pull-requests-limit: 5
    commit-message:
      prefix: deps
      include: scope

  - package-ecosystem: github-actions
    directory: /
    schedule:
      interval: weekly
    open-pull-requests-limit: 5
    commit-message:
      prefix: ci
      include: scope

  - package-ecosystem: docker
    directory: /
    schedule:
      interval: weekly
    open-pull-requests-limit: 5
    commit-message:
      prefix: docker
      include: scope
