.venv/
env/
venv/
__pycache__/
.pytest_cache/
*.pyc

# Frontend Vite: phụ thuộc, bản build và dấu vết kiểm thử trình duyệt đều dựng lại được.
/webui/node_modules/
/webui/dist/
/webui/test-results/
/webui/playwright-report/
/webui/.env*.local
/test-results/

.env
.claude/
.codex/
.agents/
.worktrees/

# Tài liệu điều phối và triển khai cục bộ không thuộc public repository.
/docs/superpowers/
/docs/deploy-cloud-run.md

checkpoints/
models/
logs/

# ``artifacts/`` chứa trọng số model, kết quả chạy cục bộ và bộ dựng hình. Chỉ bộ
# dựng hình được theo dõi: nó là mã nguồn sinh ra các ảnh trong ``docs/images/``,
# nên một bản sao mới phải dựng lại được chúng. Mọi thứ khác là kết quả chạy trên
# máy, dựng lại được bằng ``train_classifier`` và ``benchmark_classifier``.
/artifacts/*
!/artifacts/README.md
!/artifacts/figures/

/dev/
ontology.ipynb
ontology_old.ttl

# Gói kết quả mang về từ máy thuê: nhị phân, nặng, và dựng lại được từ log.
*.tar.gz

# Bảng thẻ dựng sẵn: sinh lúc dựng ảnh từ ontology + danh mục.
resources/ontology/cards.json

# Trang báo cáo xem tại máy: `report_page` dựng lại từ ontology, dataset và chỉ
# số chấm model, nên không cần giữ một bản chép trong kho.
resources/reports/bao-cao.html
